import { StyleSheet } from "react-native";

export const FooterStyles = StyleSheet.create({
    text: {
        fontSize: 14,
        color: "#fff",
        textAlign: "center",
        justifyContent: "center",
        padding: 15
    }
})